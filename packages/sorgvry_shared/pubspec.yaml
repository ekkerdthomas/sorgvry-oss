name: sorgvry_shared
description: Shared Drift schema, models, and API contracts for Sorgvry.
version: 0.2.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  drift: ^2.31.0
  sqlite3: ^2.7.4

dev_dependencies:
  build_runner: ^2.4.15
  drift_dev: ^2.31.0
  test: ^1.25.15
